This is a branch of gruntle_helper for rpi4. It is deployed on Rpi4 argon in development state - it is only for my own use, to have easy access when I feel I need to write some notes.
Currently it can be accessed on local network 192.168.1.204:8001. 
I have created a branch on github for this project - gruntle_helper_rpi4. Hmmm... just thinking, maybe it would be better to stay on the same branch but just sync common files not
specific to running system... such as all the django stuff apart from settings.py, static files etc.
TODO:
- perhaps look into deploying it via apache2
- add linux page
- would be good to re-write it so that all modules use the same base layout rather than different files for each... meh!
- see how I can code it remotely - at the moment I have VS code in rpi4 but I need to log into rpi desktop mode to use it
  
